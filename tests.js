/*
// TEST through each number to check if the statusSetter() is behaving

testAllPossibleNumbers = () => {
    return timesToExecute;
};
return  testAllPossibleNumbers(max, min); // Insert this line in the randomNumber() method*/
